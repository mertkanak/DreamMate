// DreamMate/build.gradle.kts

plugins {
    // Uygulama modülleri için plugin tanımı burada yapılmaz!
    // Sadece "buildscript" veya "plugins" bloğu ile versiyon yönetimi yapılabilir.
    // Alternatif olarak boş bırakılabilir.
}

